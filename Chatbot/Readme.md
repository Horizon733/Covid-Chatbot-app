## Chatbot
Some commands for Rasa
<br>Train: 
``` bash
rasa train
```
<br>run actions:
``` bash
rasa run actions
```
<br>run server(REST API):
``` bash
rasa run -m models --enable-api --endpoints endpoints.yml
```
<br>shell training:
 ``` bash
 rasa shell
 ```

