## Chatbot
Some commands for Rasa
Train: 
run actions:
run server(REST API):
shell training:
``` bash
rasa train
rasa run actions
rasa run -m models --enable-api --endpoints endpoints.yml
rasa shell
```
